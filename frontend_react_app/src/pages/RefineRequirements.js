import React from "react";
import { PlaceholderFeature } from "./PlaceholderFeature";

// PUBLIC_INTERFACE
export function RefineRequirements() {
  /** Placeholder page for Refine Requirements. */
  return (
    <PlaceholderFeature
      title="Refine Requirements"
      description="Turn raw requirements into testable scenarios and acceptance criteria."
      bullets={[
        "Import requirements (files, pasted text)",
        "AI suggestions for edge cases",
        "Versioning and approval workflow (future)",
      ]}
    />
  );
}
