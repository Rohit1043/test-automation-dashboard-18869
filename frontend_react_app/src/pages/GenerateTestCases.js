import React from "react";
import { PlaceholderFeature } from "./PlaceholderFeature";

// PUBLIC_INTERFACE
export function GenerateTestCases() {
  /** Placeholder page for Generate Test Cases. */
  return (
    <PlaceholderFeature
      title="Generate Test Cases"
      description="Generate structured test cases from refined requirements."
      bullets={[
        "Choose template / format",
        "Risk-based coverage suggestions",
        "Export to CSV/JSON (future)",
      ]}
    />
  );
}
